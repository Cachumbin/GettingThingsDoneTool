function Task(nombre, area, accionable) {
    this.nombre = nombre;
    this.area = area;
    this.accionable = accionable;
}

function TaskId(nombre, id) {
    this.nombre = nombre;
    this.id = id;
}