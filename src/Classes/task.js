function Task(nombre, area, accionable, categoria) {
    this.nombre = nombre;
    this.area = area;
    this.accionable = accionable;
    this.categoria = categoria;
    
}

function TaskId(nombre, elemento) {
    this.nombre = nombre;
    this.elemento = elemento;
}